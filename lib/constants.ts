// Default avatar frame overlay, used whenever a friend has no
// custom friend_frame_link uploaded. This is a site asset (not
// user content), so it lives in /public rather than Supabase Storage.
export const DEFAULT_FRAME_URL = "/assets/images/default-frame.gif";